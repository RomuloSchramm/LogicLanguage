Algoritmo "teste-repita-ate"
// repita ate é legal para quando precisa repetir a mesma coisa pelo menos uma vez para só no final ver se precisa repetir ou nao
Var
  C, F : real
  resp : caractere

Inicio

      repita
            escreva("Digite a temperatura em Celsius: ")
            leia(C)
            F <- 9*C/5+32
            escreval("Equivalente em Fahrenheit: ", F:3:1)
            escreva("Deseja repetir (s/n)? ")
            leia(resp)
      ate resp <> "s"


Fimalgoritmo
